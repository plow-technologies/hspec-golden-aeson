name:                adt-arbitrary-example
version:             0.1.0.0
github:              plow-technologies/hspec-golden-aeson
license:             BSD3
author:              James M.C. Haver II
maintainer:          mchaver@gmail.com
copyright:           BSD3

extra-source-files:
- README.md
- ChangeLog.md

# Metadata used when publishing your package
synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on Github at <https://github.com/mchaver/adt-arbitrary-test#readme>

dependencies:
- base >= 4.7 && < 5
- aeson
- QuickCheck
- quickcheck-arbitrary-adt
- hspec-golden-aeson


library:
  source-dirs: src

#executables:
#  adt-arbitrary-test-exe:
#    main:                Main.hs
#    source-dirs:         app
#    ghc-options:
#    - -threaded
#    - -rtsopts
#    - -with-rtsopts=-N
#    dependencies:
#    - adt-arbitrary-test

tests:
  test:
    main:                Spec.hs
    source-dirs:         test
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - adt-arbitrary-example
